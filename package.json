{
  "name": "rdtrans",
  "version": "1.0.0",
  "description": "mern stack web-app",
  "main": "app.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "cross-env NODE_ENV=production node app.js",
    "server": "nodemon app.js",
    "client": "npm run start --prefix client",
    "dev-start": "cross-env NODE_ENV=development concurrently \"npm run server\" \"npm run client\" ",
    "client:install": "npm install --prefix client",
    "client: build": "npm run build --prefix client"
  },
  "keywords": [
    "mern",
    "web-app"
  ],
  "author": "Kodirov Timur",
  "license": "ISC",
  "dependencies": {
    "bcrypt": "^5.0.1",
    "config": "^3.3.6",
    "cross-env": "^7.0.3",
    "express": "^4.17.1",
    "express-validator": "^6.10.1",
    "jsonwebtoken": "^8.5.1",
    "mongoose": "^5.12.5",
    "shortid": "^2.2.16"
  },
  "devDependencies": {
    "concurrently": "^6.0.2",
    "nodemon": "^2.0.7"
  }
}
